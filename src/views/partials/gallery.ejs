<section class="media">
    <h2 class="media-title sr-only">Gallery</h2>
    <div class="clearfix media-content">
        <% page.getGalleryImages().forEach(function (image, index, images) { %>
            <% include gallery-image.ejs %>
        <% }); %>
    </div>
</section>
